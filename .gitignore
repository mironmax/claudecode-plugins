# Knowledge graph data (session-specific)
.knowledge/

# Local Claude Code state; only the shared settings are tracked
.claude/*
!.claude/settings.json

# Temporary dev notes (not part of release)
knowledge-graph/devdocs/
/devdocs/

# Local promo drafts — not part of the repo
docs/promo/
